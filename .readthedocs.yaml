version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.10"

python:
  install:
    - method: poetry
      path: .
    - requirements: pyproject.toml
      method: poetry

sphinx:
  configuration: docs/conf.py
